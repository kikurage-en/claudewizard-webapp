export const template = `# 基本原則 (Evidence First / Boundary Check / Failure Reflection)

{{projectName}} における実装・変更・レビューの判断は、抽象的な「確認した」ではなく、実行前に一次証拠を示してから行う。

## 3 原則

### 1. Evidence First

外部 API / SDK / protocol / 仕様は公式ドキュメントの例だけで判断せず、raw response・欠落フィールド・error response・各フィールドの意味を実機で確認する。

- **達成条件**: 判断の根拠として、実行ログまたはツール出力の引用（ファイルパス + 行番号、またはコマンド + stdout）を判断と同じターン内に提示する
- **発動**: 「公式」「推奨」「仕様」「ベストプラクティス」「〜以下」「〜以内」を使うとき

### 2. Boundary Check

数値・設定・閾値を変更するときは、単独値ではなく組合せ境界を確認する。

- **達成条件**: 0 / 最大 / 最小 / 1 / 連続 / 停止不能 の各ケースを表または計算式で列挙する
- **発動**: 数値・設定・閾値・上限・下限を変更するとき

### 3. Failure Reflection

異常検知は通知で終わらせない。後続処理が止まるか、状態が消えるか、reconcile されるかを確認する。デプロイは build 成功ではなく動作中 artifact の反映確認まで行う。

- **達成条件**: 「検知 → 通知 → 後続処理」の 3 段が成立しているコード位置を提示する
- **発動**: エラーハンドリング / アラート / デプロイ / リトライ周りの実装時

## 運用補強条項（3 原則の適用メタルール）

- **出典明記**: 「公式」「推奨」「仕様」を使う場合は URL またはファイルパス + 行番号を併記する
- **自己適用**: 本ルールを導入・更新する作業自体にも 3 原則を適用する
- **曖昧先送り禁止**: 検出した懸念は「要修正 / 対応不要 / 保留（承認済）」のいずれかに分類する
- **WHY/HOW 分離**: 数値・閾値・パスは Truth Source（実装ファイル）を参照し、ドキュメントに直接転記しない
- **楽観判定の抑止**: 「単独」「付随」「不要」「問題ない」と断言する場合は定量根拠を併記する

## レビューでの判断基準

証拠が示されていない判断を承認しない。本原則をレビュー時の判断基準とする。
`

export default template
