export const template = `# Prevent Narrow Framing — 近視眼的判断の防止

{{projectName}} で、Claude が最初に思いついた枠のまま提案・調査・設計を進めることを防ぐためのルール。

## 発火条件

タスクが以下のいずれかを含むとき（広く解釈する）:

- 提案系: 「提案して」「どうしたらいい」「方針を考えて」
- 検証系: 「検証して」「レビューして」「正しいか確認して」
- 調査系: 「調査して」「分析して」「現状を把握して」
- 最適化系: 「最適化」「改善」「コスト削減」「高速化」
- 設計判断系: 設定値の変更・構成の変更・ツール選定

発火しない例: 単純な誤字修正、明示された 1 ファイルの編集、決定済みアクションの単純実行。

## 達成条件

1. 対象空間（軸・視点・選択肢）を列挙してから結論を出す（「検討した軸: A, B, C」の形で明示する）
2. 既存の状態を「不変」と扱わない（既存の設定値・既存のやり方も再評価候補に含める）
3. 扱わない軸も明示し、理由を 1 行添える（silent な除外をしない）
4. 作業を委任・分担するときはスコープを固定して渡さず、相手にも軸の列挙を求める
5. タスクの前提そのものが目的に照らして成立するかを、進め方を組み立てる前に一度問う

## 自己適用

本ルールを導入・更新する作業自体にも上記を適用する。
`

export default template
