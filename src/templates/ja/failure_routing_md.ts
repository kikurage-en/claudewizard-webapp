export const template = `# Failure Routing — 再発防止策の置き場所

{{projectName}} で発生した失敗・指摘の再発防止策を、CLAUDE.md へ常時追記してコンテキストを肥大させる前に、最小の有効な置き場所へ振り分けるためのルール。

## 発火条件

- 失敗・レビュー指摘・事故から再発防止策を決めるとき
- CLAUDE.md / rules への新規追加を検討するとき
- 「今後は気をつける」「ルール化する」と言いそうになったとき

## 達成条件

再発防止策は、まず失敗タイプごとに最小の有効な置き場所へ逃がす。

| 失敗タイプ | 最初に置く場所 |
| --- | --- |
| 外部仕様・API の誤認 | テスト / 検証スクリプト |
| 数値・閾値の誤り | バリデーション / テスト |
| 反映漏れ・手順抜け | チェックリスト / スクリプト化 |
| ドキュメントのずれ | 整合チェック / 参照先の一本化 |
| 秘密情報の混入 | 権限設定 / 機械的スキャン |

手順:

1. 失敗タイプを分類する
2. テスト・スクリプト・チェックリストで防げるかを先に判断する
3. 常時コンテキスト（CLAUDE.md / rules）に入れずに防げる置き場所を採用する
4. 採用先と、常時ルール化しない理由を明示する

## 昇格条件

CLAUDE.md / rules への常時追加は、同型の失敗が繰り返し発生している・機械的に防げない・影響が重大、のいずれかを満たす場合に限る。昇格する場合も事故の詳細は転記せず、抽象化した判断基準のみ記載する。

## 自己適用

新しい失敗知識を追加するときは、本ファイルで分類してから置き場所を決める。
`

export default template
